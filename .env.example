# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars

# Admin Configuration
ADMIN_PASSWORD=changeme123

# SMTP Configuration (already configured via IP auth)
# No changes needed unless you want to use different SMTP server

# Domain Configuration
DOMAIN=marketing.myndsolution.com
